# Bootstrap Grammar Authoring Task

You are building the initial manifest grammar for the Cookiecutter agentic fuzzer.

Constraints:

- Target local templates only.
- Emit a manifest DSL that materializes into a temporary template repository and invocation spec.
- Keep top-level manifest structure stable.
- Prefer valid scenarios first, then controlled edge cases.

Deliverables:

1. Update the grammar at `{grammar_path}`.
2. Explain protected rules and editable rules.
3. List seeded Cookiecutter features and deferred features.

